import { Component, ReactNode } from "react";
import { TabPageSelectorPreviewProps } from "../typings/TabPageSelectorProps";

declare function require(name: string): string;

export class preview extends Component<TabPageSelectorPreviewProps> {
    render(): ReactNode {
        return "";
    }
}
